@model thewall9.data.binding.PageWeb

@section styles{

}
@section scripts{
    @*<script src="~/scripts/min/home-vendors.js"></script>*@
}
<section id="home">
    <div class="wrapper" id="landing" style="background-image:url(@Html.FindValue(Model,"bg")">
        <div class="wrapper-container">
            <div class="container">
                <h1>
                    @Html.FindValue(Model, "h1")
                </h1>
                <h2>
                    @Html.FindValue(Model, "h2")
                </h2>
                <p>
                    <a href="@Html.FindValue(Model, "contact-btn-url")" class="btn btn-lg btn-primary">
                        @Html.FindValue(Model, "contact-btn-text")
                    </a>
                </p>
            </div>
        </div>
    </div>
</section>
